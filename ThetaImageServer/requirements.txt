# Core dependencies
numpy>=1.24,<2.0
opencv-python>=4.9
pyvirtualcam>=0.11

# Fast camera capture via FFmpeg DirectShow (recommended; ~2-3x FPS vs OpenCV.
# server.py automatically falls back to OpenCV capture if not installed)
av>=12

# PyTorch inference backend (CUDA GPU / CPU).
# Also required once for ONNX export (--export-onnx).
torch>=2.3

# --- Optional: non-NVIDIA GPU inference (AMD/Intel via DirectML on Windows) ---
# pip install onnxruntime-directml   # any DirectX 12 GPU
# pip install onnxruntime            # CPU-only alternative
# pip install onnx                   # required only when running --export-onnx

# --- Dev / benchmark only ---
# scipy>=1.10          # legacy scipy-based E2C reference in benchmark_stages.py
# flask                # server_png.py (legacy HTTP PNG streaming variant)
# torchvision          # server_png.py (legacy)
